<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/output.css" />
  </head>
  <body class="bg-slate-300">
    <form action="register" method="POST" class="flex items-center justify-center h-[100vh]">
      <div class="flex flex-col items-center justify-center w-2/5 gap-5 py-10 bg-white rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold text-black">Sign Up</h1>
        <div class="w-full px-10">
          <div class="flex flex-col w-full gap-1">
            <label for="username" class="">Masukkan Username :</label>
            <input type="text" placeholder="masukkan username" name="username" class="px-2 py-1 border border-black rounded-md" required />
          </div>
          <div class="flex flex-col gap-1 mt-3">
            <label for="password">Masukkan password :</label>
            <input type="password" placeholder="masukkan password" name="password" class="px-2 py-1 border border-black rounded-md" required />
          </div>
          <div class="flex flex-col gap-1 mt-3">
            <label for="password">Konfirmasi password :</label>
            <input type="password" placeholder="konfirmasi password" name="password2" class="px-2 py-1 border border-black rounded-md" required />
          </div>
          <% if (typeof(error) != "undefined") { %>
          <p class="pt-2 text-sm text-red-500"><%= error %></p>
          <% } %>
          <button class="w-full py-1 mt-4 text-white bg-blue-500 rounded-lg shadow-md">Submit</button>
          <p class="mt-3 text-sm text-center text-opacity-50">Already have an account? <a href="/login" class="text-blue-500">Login</a></p>
        </div>
      </div>
    </form>
  </body>
</html>
